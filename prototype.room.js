
module.exports = function() {
    /*
    Room.prototype.GetTicksWaitingForEnergy: function() {
        return this.memory.TicksWaitingForEnergy ? this.memory.TicksWaitingForEnergy : 0; 
    },
    Room.prototype.AddTicksWaitingForEnergy: function(ticks=1) {
        if (this.memory.TicksWaitingForEnergy)
            this.memory.TicksWaitingForEnergy += ticks;
        else
            this.memory.TicksWaitingForEnergy = ticks;
    },
    Room.prototype.SetTicksWaitingForEnergy: function(ticks) {
        this.memory.TicksWaitingForEnergy = ticks;
    }
    Room.prototype.GetTicksOnFullEnergy: function() {
        return this.memory.TicksOnFullEnergy ? this.memory.TicksOnFullEnergy : 0; 
    },
    Room.prototype.AddTicksOnFullEnergy: function(ticks=1) {
        if (this.memory.TicksOnFullEnergy)
            this.memory.TicksOnFullEnergy += ticks;
        else
            this.memory.TicksOnFullEnergy = ticks;
    },
    Room.prototype.SetTicksOnFullEnergy: function(ticks) {
        this.memory.TicksOnFullEnergy = ticks;
    }
    */
    // TODO добавить функции учета времени которое источник ресурсов проводит в регене без ресурсов
};